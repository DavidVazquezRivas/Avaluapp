export const PublicRoutes = {
  Login: '/login',
}

export const PrivateRoutes = {
  Private: '/private',
}

export const UserRoutes = {
  Base: '/user',
  Dashboard: 'dashboard',
}

export const AdminRoutes = {
  Base: '/admin',
  Dashboard: 'dashboard',
  Users: 'users',
}
